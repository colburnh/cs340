<body>

<!-- jumbotron to provide info about page -->
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Find out how much your pet should eat</h1>
    <p class="lead">This page will:</p>
    <ul>
    <li> List, add, and delete products </li>
    <li> Search products by brand name </li>
  </ul>
  </div>
</div>

<h3>Add Products:</h3>
<form id="addProduct" action="/products" method="post">
    Health Issue: <select name="healthIssue"> 
        {{#each healthIssues}}
        <option value="{{healthIssueID}}">{{healthIssue}} (for {{species}})</option>
        {{/each}}
    </select><br>
    For Species: <input type="radio" name="forSpecies" value="Cat">
    <label for="Cat">Cat</label>
    <input type="radio" name="forSpecies" value="Dog">
    <label for="Dog">Dog</label><br>
    Brand Name: <input type="text" name="brandName"><br>
    Food Name: <input type="text" name="foodName"><br>
    Food Type: <input type="text" name="foodType"><br>
    Unit: <input type="text" name="unit"><br>
    Calories Per Unit: <input type="number" name="calPerUnit"><br>
    <input type="submit" value="Submit">
 </form>
<br/>

<form id='searchProducts' action='' method=''>
<!--- this form does not submit directly, the button handles all the interaction with backend -->
    <label> Search products by brand name:</label>
    <input id='brandNameString' type='text' name='brandNameString' value=''>
    <input type="button" value="Search" onclick="searchProductsByBrandName()">
</form>

<h3>Current Products:</h3>
<table style="width:900px">
    <thead>
        <tr style="border:1px solid black">
            <th>Health Issue</th>
            <th>For Species</th> 
            <th>Brand Name</th> 
            <th>Food Name</th>
            <th>Food Type</th>
            <th>Unit</th>
            <th>Cal Per Unit</th>
            <th>Delete</th>
        </tr>
    </thead> 
    <tbody>
        {{#each products}} 
        <tr style="border:1px solid black"> 
            <td>{{healthIssue}}</td>
            <td>{{forSpecies}}</td> 
            <td>{{brandName}}</td> 
            <td>{{foodName}}</td> 
            <td>{{foodType}}</td> 
            <td>{{unit}}</td> 
            <td>{{calPerUnit}}</td> 
            <td><button onclick="deleteProduct({{productID}})">Delete</button></td>
        </tr> 
        {{/each}} 
    </tbody> 
</table> 
</body>