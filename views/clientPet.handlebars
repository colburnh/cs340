<body>

<!-- jumbotron to provide info about page -->
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Find out how much your pet should eat</h1>
    <p class="lead">This page will:</p>
    <ul>
    <li> List the 1:M clientPet table (see what client owns what pets) </li>
    <li>add and delete clientPet relationships  </li>
  </ul>
  <p class="lead">One client can own many pets, but one pet can owned by only one client. As such, if all pets currently have an owner, you won't be able to add an entry to the table.</p>
  </div>
</div>

<h3>Add a Client Pet Relationship:</h3>
<form id="addClientPet" action="/clientPet" method="post">
    Select a Client: <select name="clientID"> 
        {{#each clients}}
        <option value="{{clientID}}">{{fname}} {{lname}}</option>
        {{/each}}
    </select><br>
    Select a Pet: <select name="petID"> 
        {{#each pets}}
        <option value="{{petID}}">{{petName}}</option>
        {{/each}}
    </select><br>
    <input type="submit" value="Submit">
 </form>
<br/>

<h3>Current Client Pet Relationships:</h3>
<table style="width:500px">
    <thead>
        <tr style="border:1px solid black">
            <th>Client</th>
            <th>Pet</th>
            <th>Delete</th>
        </tr>
    </thead> 
    <tbody>
        {{#each clientPet}} 
        <tr style="border:1px solid black"> 
            <td>{{name}}</td>
            <td>{{petName}}</td>
            <td><button onclick="deleteClientPet({{petID}})">Delete</button></td>
        </tr> 
        {{/each}} 
    </tbody> 
</table> 
</body>
